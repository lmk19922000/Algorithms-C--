#include "stdio.h"
#include "iostream"
#include "string"

using namespace std;

int main(){
	string num;
	int numQuery, x, y, temp;
	char c;

	while (getline(cin, num)){
		scanf("%d", &numQuery);
		for (int i = 0; i < numQuery; i++){
			scanf("%d", &x);
			scanf("%d", &y);
			if (x>y){
				temp = x;
				x = y;
				y = temp;
			}
			c = num.at(0);
			for (int j = x; j <= y; j++){

			}
		}
	}

	return 0;
}